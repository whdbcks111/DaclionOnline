# Combat Master Data

투사체, 스킬, 상태효과, 속성 상성, 보스 서사와 패턴을 정적 registry에 등록한다. 실행 규칙은 `models/combat`가 소유하고 이 폴더는 안정 ID와 밸런스 정의를 제공한다.

참조 순서가 있는 데이터는 [`../../index.ts`](../../index.ts)의 import 순서를 함께 확인한다.
